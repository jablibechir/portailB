# =============================================
#  Profil TEST ? Portail Recrutement
#  Utilisé par Testcontainers (DB éphémère)
# =============================================

# Désactive la datasource auto ? Testcontainers la fournit dynamiquement
spring.datasource.url=
spring.datasource.username=
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# JWT ? valeurs fixes pour les tests
jwt.secret=test-secret-key-must-be-at-least-32-chars!!
jwt.access-token-expiration-ms=900000
jwt.refresh-token-expiration-ms=604800000

# Désactive le Python microservice en test
python.service.base-url=http://localhost:9999

logging.level.com.soprarh=DEBUG
logging.level.org.springframework.security=DEBUG

